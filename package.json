{
  "name": "fingyaan",
  "version": "1.0.0",
  "description": "Student-first budgeting, investing, and learning web app (static HTML/CSS/JS).",
  "private": true,
  "scripts": {
    "dev": "http-server -p 8000 .",
    "lint:js": "eslint \"assets/js/**/*.js\" --max-warnings=0",
    "lint:css": "stylelint \"assets/css/**/*.css\" --max-warnings=0",
    "format": "prettier --write .",
    "test": "jest",
    "test:e2e": "playwright test",
    "prepare": "playwright install --with-deps || true",
    "icons": "node tools/gen-icons.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "jsdom": "^24.0.0",
    "playwright": "^1.47.0",
    "prettier": "^3.3.3",
    "stylelint": "^16.9.0",
    "stylelint-config-standard": "^36.0.0",
    "sharp": "^0.33.4"
  }
}
